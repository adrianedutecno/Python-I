#los modulos son importados dentro del proyecto, como librerias, que realizan alguna accion
#Ejemplo modulo math
#se importan en la parte superior del proyecto, como la libreria numpy, scipy, matplotlib
from math import pi as PI_VALUE #importando una libreria especifica de math, as (es para asignar un alias)
from math import * #importando todo lo que contiene la libreria math, * asterisco significa todos los elementos
import math #importando la libreria math

#la libreria math es una libreria de operaciones matematicas o aritmeticas
#provee algunas funciones o metodos
print(math.sqrt(2)) #calcula la raiz cuadrada de 2
print(math.exp(2)) #calcula el exponente de 2
print(math.pow(2,3)) #calcula la potencia de 2 a 3
print(math.log(2)) #calcula la logaritmo de 2

